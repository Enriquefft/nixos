---
phase: 02-scaffolding-and-identity
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - /etc/nixos/zeroclaw/documents/IDENTITY.md
  - /etc/nixos/zeroclaw/documents/SOUL.md
  - /etc/nixos/zeroclaw/documents/TOOLS.md
  - /etc/nixos/zeroclaw/documents/USER.md
  - /etc/nixos/zeroclaw/documents/LORE.md
autonomous: true
requirements: [IDN-01, IDN-02]

must_haves:
  truths:
    - "Zero occurrences of 'openclaw' (case-insensitive) in IDENTITY.md, SOUL.md, TOOLS.md, USER.md, LORE.md"
    - "SOUL.md Cron Jobs section references zeroclaw cron CLI, not YAML workflow or openclaw cron"
    - "TOOLS.md Cron Management section references zeroclaw cron CLI"
    - "TOOLS.md Utility Skills table reflects current ZeroClaw state (find-skills, skill-creator preloaded — other skills are v2 scope)"
    - "All /etc/nixos/openclaw/ paths updated to /etc/nixos/zeroclaw/ across all 5 documents"
    - "USER.md and LORE.md path references updated (full-profile.md, reusable-responses.md paths corrected)"
  artifacts:
    - path: "/etc/nixos/zeroclaw/documents/IDENTITY.md"
      provides: "Kiro's identity — updated to reference ZeroClaw"
    - path: "/etc/nixos/zeroclaw/documents/SOUL.md"
      provides: "Kiro's behavioral values — cron section rewritten for zeroclaw CLI"
    - path: "/etc/nixos/zeroclaw/documents/TOOLS.md"
      provides: "Tool reference — zeroclaw paths and cron/skills sections updated"
    - path: "/etc/nixos/zeroclaw/documents/USER.md"
      provides: "User profile — path reference updated"
    - path: "/etc/nixos/zeroclaw/documents/LORE.md"
      provides: "Context and lore — paths and product references updated"
  key_links:
    - from: "SOUL.md Cron Jobs section"
      to: "zeroclaw cron CLI"
      via: "rewritten cron workflow referencing zeroclaw cron add"
      pattern: "zeroclaw cron"
    - from: "TOOLS.md Cron Management section"
      to: "zeroclaw cron CLI"
      via: "full rewrite replacing cron-manager with zeroclaw cron"
      pattern: "zeroclaw cron"
---

<objective>
Surgical audit and update of the remaining 5 identity documents (IDENTITY, SOUL, TOOLS, USER, LORE) to remove all stale OpenClaw references.

Purpose: These 5 documents are loaded by ZeroClaw at runtime and shape how Kiro understands its own context, tools, and environment. Stale OpenClaw paths and tool names cause Kiro to use tools that don't exist or reference paths that don't resolve.

Output: 5 updated identity documents — clean break from OpenClaw, all paths and tool names reflect ZeroClaw
</objective>

<execution_context>
@/home/hybridz/.claude/get-shit-done/workflows/execute-plan.md
@/home/hybridz/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/etc/nixos/zeroclaw/.planning/ROADMAP.md
@/etc/nixos/zeroclaw/.planning/phases/02-scaffolding-and-identity/02-RESEARCH.md
@/etc/nixos/zeroclaw/documents/IDENTITY.md
@/etc/nixos/zeroclaw/documents/SOUL.md
@/etc/nixos/zeroclaw/documents/TOOLS.md
@/etc/nixos/zeroclaw/documents/USER.md
@/etc/nixos/zeroclaw/documents/LORE.md

<interfaces>
<!-- Per-document change map from RESEARCH.md identity audit findings -->
<!-- Read each document first, then apply only the changes listed below. -->

IDENTITY.md — 1 change:
- Line 9: "You run 24/7 on Enrique's personal NixOS workstation via OpenClaw." → replace "OpenClaw" with "ZeroClaw"

SOUL.md — multiple changes in System Access section + Cron Jobs section:
- System Access, 3 path fixes:
  - "Before modifying anything inside /etc/nixos/openclaw/, read /etc/nixos/openclaw/CLAUDE.md first" → replace both openclaw paths with zeroclaw
  - "OpenClaw config: /etc/nixos/openclaw" → "ZeroClaw config: /etc/nixos/zeroclaw"
  - "Workspace files: /etc/nixos/openclaw/documents/" → "/etc/nixos/zeroclaw/documents/"
  - "Full profile reference: /etc/nixos/openclaw/reference/full-profile.md" → "/etc/nixos/zeroclaw/reference/full-profile.md" (note: file does not yet exist — add "(not yet migrated)" annotation)
- Cron Jobs section — full rewrite:
  - Remove: all YAML cron workflow (cron/jobs/*.yaml, cron-sync, openclaw cron list)
  - Remove: export $(cat /run/secrets/rendered/openclaw.env | xargs) — this env var doesn't apply
  - Replace with: zeroclaw cron add/list/remove/pause/resume workflow using the verified CLI commands from RESEARCH.md
  - Keep: the spirit of the section (cron is how routine work runs) — just update the mechanics

TOOLS.md — multiple changes across several sections:
- Claude Code — Self-repair: "fix broken/stubbed skills in /etc/nixos/openclaw/skills/" → /etc/nixos/zeroclaw/skills/
- File System section: all /etc/nixos/openclaw/ paths → /etc/nixos/zeroclaw/
- systemctl Key services: "openclaw-gateway" → "zeroclaw-gateway"
- journalctl: "journalctl --user -u openclaw-gateway -f" → "zeroclaw-gateway"
- Self-Modification section: all openclaw paths → zeroclaw; "Run `up` to apply" → note that documents/ changes are immediately live (no rebuild), module.nix changes require nixos-rebuild switch
- Cron Management section — full rewrite: replace cron-manager, YAML workflow, OpenClaw paths with zeroclaw cron CLI workflow (same pattern as SOUL.md Cron Jobs fix)
- Utility Skills table — update to reflect current state:
  - Keep: find-skills, skill-creator (these are preloaded in ZeroClaw workspace)
  - Remove or clearly mark as v2 scope: cron-manager, skill-scaffold, job-scanner, rss-reader, job-tracker, git-activity, task-queue — these do not exist in ZeroClaw
  - Replace section with current state: 2 preloaded skills (find-skills, skill-creator); Kiro-authored skills go in /etc/nixos/zeroclaw/skills/ and are deployed via zeroclaw skills install
  - Remove: "State files: ~/.local/state/openclaw-cron/<skill-name>/" — not applicable

USER.md — 1 change:
- "The complete detailed profile lives at /etc/nixos/openclaw/reference/full-profile.md." → replace path with /etc/nixos/zeroclaw/reference/full-profile.md and add "(not yet migrated)" annotation

LORE.md — several changes:
- Resume/CV Notes: 2 path refs → /etc/nixos/zeroclaw/reference/full-profile.md and /etc/nixos/zeroclaw/reference/reusable-responses.md (both with "(not yet migrated)" annotation — files confirmed absent)
- Application Tracker: "~/openclaw-data/job-tracker.json" → "~/zeroclaw-data/job-tracker.json"
- Freelance Tracker: "~/openclaw-data/freelance-tracker.json" → "~/zeroclaw-data/freelance-tracker.json"
- Products — "OpenClaw + Nix Automation (Kiro)" → "ZeroClaw + Nix Automation (Kiro)"
- Products — "OpenClaw Kapso WhatsApp Bridge" → "Kapso WhatsApp Bridge" (now standalone project)
- Distribution narrative: "automating his entire job search and content pipeline with OpenClaw on NixOS" → replace "OpenClaw" with "ZeroClaw"
- Key Stories: reference path → /etc/nixos/zeroclaw/reference/full-profile.md (not yet migrated)

All changes: surgical. Preserve voice, tone, formatting, and all non-OpenClaw content.
No backwards compatibility notes. No migration stubs. Write as if ZeroClaw is the only system.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix IDENTITY.md and USER.md — surgical path updates</name>
  <files>/etc/nixos/zeroclaw/documents/IDENTITY.md, /etc/nixos/zeroclaw/documents/USER.md</files>
  <action>
Apply the minimum required changes to IDENTITY.md and USER.md. Read each file first before editing.

**IDENTITY.md** (1 change):
- Find the sentence containing "You run 24/7 on Enrique's personal NixOS workstation via OpenClaw."
- Replace "OpenClaw" with "ZeroClaw"
- No other changes

**USER.md** (1 change):
- Find the sentence referencing the full profile at /etc/nixos/openclaw/reference/full-profile.md
- Update path to /etc/nixos/zeroclaw/reference/full-profile.md
- Append "(not yet migrated)" to the same sentence — the file is confirmed absent from /etc/nixos/zeroclaw/reference/
- No other changes

Preserve all other content, formatting, and structure exactly.
  </action>
  <verify>
    <automated>grep -ri "openclaw" /etc/nixos/zeroclaw/documents/IDENTITY.md && echo "FAIL: openclaw in IDENTITY" || echo "PASS: IDENTITY clean" && grep -ri "openclaw" /etc/nixos/zeroclaw/documents/USER.md && echo "FAIL: openclaw in USER" || echo "PASS: USER clean"</automated>
  </verify>
  <done>IDENTITY.md references ZeroClaw; USER.md path updated to /etc/nixos/zeroclaw/reference/full-profile.md with "(not yet migrated)" note; zero openclaw refs in both files</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite SOUL.md — fix paths and cron section</name>
  <files>/etc/nixos/zeroclaw/documents/SOUL.md</files>
  <action>
Read `/etc/nixos/zeroclaw/documents/SOUL.md` end-to-end first. Then apply the changes listed in the interfaces block.

**System Access section** — 4 path fixes:
1. Replace all /etc/nixos/openclaw/ paths with /etc/nixos/zeroclaw/
2. "OpenClaw config: /etc/nixos/openclaw" → "ZeroClaw config: /etc/nixos/zeroclaw"
3. "Workspace files: /etc/nixos/openclaw/documents/" → "/etc/nixos/zeroclaw/documents/"
4. "Full profile reference: /etc/nixos/openclaw/reference/full-profile.md" → "/etc/nixos/zeroclaw/reference/full-profile.md (not yet migrated)"

**Cron Jobs section** — full rewrite of the mechanics (not the spirit):
- Remove: YAML cron workflow (cron/jobs/*.yaml, cron-sync, openclaw cron list commands)
- Remove: openclaw.env export line
- Replace with: A clear explanation that cron is managed entirely via zeroclaw CLI (SQLite-backed, no files); include the key commands:
  - zeroclaw cron add '0 9 * * *' --tz 'America/Lima' 'agent -m "..."' (daily session)
  - zeroclaw cron list (see jobs and their IDs)
  - zeroclaw cron pause/resume/remove <id>
- Keep the section's intent: explain that cron is how routine work runs and how Kiro automates recurring tasks
- Do not add more than a short paragraph + the CLI commands — this section describes behavior, not a full tutorial

All other sections (personality, voice, values, etc.) — preserve exactly. Zero other changes.
  </action>
  <verify>
    <automated>grep -ri "openclaw" /etc/nixos/zeroclaw/documents/SOUL.md && echo "FAIL: openclaw in SOUL" || echo "PASS: SOUL clean" && grep -q "zeroclaw cron" /etc/nixos/zeroclaw/documents/SOUL.md && echo "PASS: zeroclaw cron in SOUL" || echo "FAIL: zeroclaw cron missing from SOUL"</automated>
  </verify>
  <done>SOUL.md has zero openclaw refs; System Access section paths all point to /etc/nixos/zeroclaw/; Cron Jobs section references zeroclaw cron CLI; all other content preserved</done>
</task>

<task type="auto">
  <name>Task 3: Update TOOLS.md and LORE.md — section rewrites and path fixes</name>
  <files>/etc/nixos/zeroclaw/documents/TOOLS.md, /etc/nixos/zeroclaw/documents/LORE.md</files>
  <action>
Read both files end-to-end first. Then apply the changes listed in the interfaces block.

**TOOLS.md** — apply in this order:
1. All /etc/nixos/openclaw/ paths → /etc/nixos/zeroclaw/ (global find-and-replace)
2. "openclaw-gateway" → "zeroclaw-gateway" (in systemctl and journalctl sections)
3. Self-Modification section: "Run `up` to apply" → replace with accurate deployment note:
   - documents/ changes: immediately live (no rebuild — symlink passes through to source)
   - module.nix changes: require nixos-rebuild switch
4. Cron Management section — full rewrite using zeroclaw cron CLI (same pattern as SOUL.md Cron Jobs fix):
   - Remove: cron-manager, YAML workflow, openclaw paths
   - Replace with: zeroclaw cron add/list/pause/resume/remove workflow with examples
5. Utility Skills section — replace with current ZeroClaw state:
   - Remove the OpenClaw skills table (cron-manager, skill-scaffold, job-scanner, rss-reader, job-tracker, git-activity, task-queue)
   - Replace with a brief section noting: 2 preloaded ZeroClaw skills (find-skills: finds relevant skills by query; skill-creator: guides skill creation); Kiro-authored skills live in /etc/nixos/zeroclaw/skills/ and are installed via zeroclaw skills install; additional skills are v2 scope
   - Remove: "State files: ~/.local/state/openclaw-cron/<skill-name>/" line

**LORE.md** — apply the 7 changes listed in the interfaces block:
1. Resume/CV Notes: both path refs to /etc/nixos/zeroclaw/reference/ (with "(not yet migrated)" note)
2. Application Tracker path: ~/openclaw-data/ → ~/zeroclaw-data/
3. Freelance Tracker path: ~/openclaw-data/ → ~/zeroclaw-data/
4. Products: "OpenClaw + Nix Automation (Kiro)" → "ZeroClaw + Nix Automation (Kiro)"
5. Products: "OpenClaw Kapso WhatsApp Bridge" → "Kapso WhatsApp Bridge"
6. Distribution narrative: "with OpenClaw on NixOS" → "with ZeroClaw on NixOS"
7. Key Stories: reference path → /etc/nixos/zeroclaw/reference/full-profile.md (not yet migrated)

All other content in both files — preserve exactly. Voice, tone, formatting unchanged.
  </action>
  <verify>
    <automated>grep -ri "openclaw" /etc/nixos/zeroclaw/documents/TOOLS.md && echo "FAIL: openclaw in TOOLS" || echo "PASS: TOOLS clean" && grep -ri "openclaw" /etc/nixos/zeroclaw/documents/LORE.md && echo "FAIL: openclaw in LORE" || echo "PASS: LORE clean" && grep -q "zeroclaw cron" /etc/nixos/zeroclaw/documents/TOOLS.md && echo "PASS: zeroclaw cron in TOOLS" || echo "FAIL: zeroclaw cron missing from TOOLS"</automated>
  </verify>
  <done>TOOLS.md and LORE.md have zero openclaw refs; TOOLS.md cron section references zeroclaw cron CLI; Utility Skills section reflects current ZeroClaw state; LORE.md paths and product names updated to zeroclaw</done>
</task>

</tasks>

<verification>
After all three tasks complete:

```bash
# Zero openclaw refs in all 5 documents (the definitive test for IDN-01)
grep -ri "openclaw" /etc/nixos/zeroclaw/documents/IDENTITY.md /etc/nixos/zeroclaw/documents/SOUL.md /etc/nixos/zeroclaw/documents/TOOLS.md /etc/nixos/zeroclaw/documents/USER.md /etc/nixos/zeroclaw/documents/LORE.md && echo "FAIL: openclaw refs remain" || echo "PASS: all 5 docs clean"

# ZeroClaw-native cron content
grep -q "zeroclaw cron" /etc/nixos/zeroclaw/documents/SOUL.md && echo "SOUL cron OK"
grep -q "zeroclaw cron" /etc/nixos/zeroclaw/documents/TOOLS.md && echo "TOOLS cron OK"

# Runtime sanity
zeroclaw skills list && zeroclaw cron list

# Full wave audit (also covers AGENTS.md from plan 02)
grep -ri "openclaw" /etc/nixos/zeroclaw/documents/ && echo "WAVE AUDIT FAIL: openclaw refs remain" || echo "WAVE AUDIT PASS: all 6 docs clean"
```
</verification>

<success_criteria>
1. Zero openclaw references across all 5 documents (grep -ri returns nothing)
2. SOUL.md Cron Jobs section contains zeroclaw cron CLI commands
3. TOOLS.md Cron Management section contains zeroclaw cron CLI commands
4. TOOLS.md Utility Skills section reflects ZeroClaw's current skill state (find-skills, skill-creator preloaded)
5. All path references in all 5 documents point to /etc/nixos/zeroclaw/
6. Combined with plan 02-02 output: all 6 identity documents are openclaw-free (IDN-01 complete)
7. zeroclaw skills list and zeroclaw cron list return without errors (IDN-02 runtime validation)
</success_criteria>

<output>
After completion, create `/etc/nixos/zeroclaw/.planning/phases/02-scaffolding-and-identity/02-03-SUMMARY.md`
</output>
