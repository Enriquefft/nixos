---
phase: 02-scaffolding-and-identity
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - /etc/nixos/zeroclaw/skills/README.md
  - /etc/nixos/zeroclaw/cron/README.md
  - /etc/nixos/zeroclaw/CLAUDE.md
autonomous: true
requirements: [DIR-01, DIR-02, DIR-03, MOD-03]

must_haves:
  truths:
    - "Any agent reading skills/README.md can create, audit, install, and verify a skill using zeroclaw CLI without asking for help"
    - "Any agent reading cron/README.md can add, list, pause, resume, and remove cron jobs using zeroclaw CLI without asking for help"
    - "Any agent reading CLAUDE.md knows which files require a NixOS rebuild vs which take effect immediately"
    - "CLAUDE.md contains a complete file map of every important file and directory in the repo"
  artifacts:
    - path: "/etc/nixos/zeroclaw/skills/README.md"
      provides: "Full operational guide for skill creation and deployment via zeroclaw CLI"
    - path: "/etc/nixos/zeroclaw/cron/README.md"
      provides: "Full operational guide for cron job management via zeroclaw CLI"
    - path: "/etc/nixos/zeroclaw/CLAUDE.md"
      provides: "Agent guidance covering deployment model, file map, and operational workflows"
  key_links:
    - from: "/etc/nixos/zeroclaw/CLAUDE.md"
      to: "rebuild vs live-edit table"
      via: "explicit table listing every key file and its deployment model"
      pattern: "rebuild|live.edit|mkOutOfStoreSymlink"
    - from: "/etc/nixos/zeroclaw/skills/README.md"
      to: "zeroclaw CLI commands"
      via: "zeroclaw skills audit/install/list workflow"
      pattern: "zeroclaw skills (audit|install|list)"
    - from: "/etc/nixos/zeroclaw/cron/README.md"
      to: "zeroclaw CLI commands"
      via: "zeroclaw cron add/list/remove workflow"
      pattern: "zeroclaw cron (add|list|remove|pause|resume)"
---

<objective>
Create three new documentation files that give any agent working on this repo complete operational guidance.

Purpose: Phase 2's scaffolding layer — skills/README.md and cron/README.md enable Kiro to manage its own skills and cron jobs autonomously; CLAUDE.md enables any coding agent or Kiro to understand the deployment model without prior context.

Output: Three new files — skills/README.md, cron/README.md, CLAUDE.md
</objective>

<execution_context>
@/home/hybridz/.claude/get-shit-done/workflows/execute-plan.md
@/home/hybridz/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/etc/nixos/zeroclaw/.planning/PROJECT.md
@/etc/nixos/zeroclaw/.planning/ROADMAP.md
@/etc/nixos/zeroclaw/.planning/phases/02-scaffolding-and-identity/02-RESEARCH.md
@/etc/nixos/zeroclaw/.planning/phases/01-config-foundation/01-01-SUMMARY.md

<interfaces>
<!-- Key patterns from Phase 1. Executor should use these directly. -->

From module.nix (deployment model — what is live-edit vs rebuild):
- documents/*.md: live edit via mkOutOfStoreSymlink → ~/.zeroclaw/documents/
- workspace/SOUL.md, workspace/AGENTS.md: live edit via mkOutOfStoreSymlink → /etc/nixos/zeroclaw/documents/
- reference/: live edit via mkOutOfStoreSymlink → /etc/nixos/zeroclaw/reference/
- config.toml: rendered by Nix at build time (pkgs.writeText) — rebuild required
- skills/, cron/: placeholder dirs only, no symlinks — deployed via zeroclaw CLI
- flake.nix: rebuild required + direnv reload

Verified CLI (from RESEARCH.md, HIGH confidence):
- zeroclaw skills list       → shows installed skills
- zeroclaw skills audit <path> → security check before install
- zeroclaw skills install <path> → installs from local path
- zeroclaw skills remove <name>
- zeroclaw cron add '<expr>' [--tz <TZ>] '<command>'
- zeroclaw cron add-every <ms> '<command>'
- zeroclaw cron add-at '<rfc3339>' '<command>'
- zeroclaw cron once <delay> '<command>'
- zeroclaw cron list
- zeroclaw cron pause <id>
- zeroclaw cron resume <id>
- zeroclaw cron remove <id>

Skill formats (RESEARCH.md):
SKILL.md: YAML frontmatter (name, description) + markdown instructions — recommended for knowledge skills
SKILL.toml: [[tools]] registration for custom shell tools — use when registering CLI tools

Skill directory structure:
skills/my-skill/
├── SKILL.md          # required
├── scripts/          # optional — executables
├── references/       # optional — context docs (NO symlinks inside)
└── assets/           # optional — templates

Cron backend: SQLite at ~/.zeroclaw/workspace/cron/jobs.db
- cron.enabled defaults to true — no config change needed
- Commands can be shell or 'agent -m "prompt"' for AI sessions
- Timezone: --tz IANA_TZ flag

Anti-patterns: No YAML cron files. No cron-sync. No skill-scaffold. Never write to jobs.db directly.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create skills/README.md — full operational guide</name>
  <files>/etc/nixos/zeroclaw/skills/README.md</files>
  <action>
Create `/etc/nixos/zeroclaw/skills/README.md` as a complete operational guide. Kiro must be able to create, audit, install, and verify a skill by reading this file alone.

Required sections:
1. Overview — two supported formats (SKILL.md for knowledge skills, SKILL.toml for tool registration), when to use each, where installed skills live (~/.zeroclaw/workspace/skills/)
2. Directory structure — annotated tree showing SKILL.md (required), scripts/, references/, assets/ (all optional); note that no symlinks allowed inside skill packages (zeroclaw audit will reject them)
3. SKILL.md format — full annotated example with frontmatter (name, description) and markdown body; explain that these fields are injected into agent system prompt at runtime
4. SKILL.toml format — full annotated example with [skill] block (name, description, version, author, tags) and [[tools]] block (name, description, kind="shell", command); explain when this is needed vs SKILL.md
5. Workflow — numbered steps: mkdir -p skills/my-skill → write SKILL.md → zeroclaw skills audit → zeroclaw skills install → zeroclaw skills list → git add + commit; explain that source of truth is /etc/nixos/zeroclaw/skills/, installed workspace is ~/.zeroclaw/workspace/skills/
6. Reference — CLI quick reference table (zeroclaw skills list/audit/install/remove)

Use the examples from RESEARCH.md exactly (morning-briefing SKILL.md example, job-scanner SKILL.toml example). Section ordering is at executor's discretion. Do not include zeroclaw cron or CLAUDE.md content — that's in other tasks.
  </action>
  <verify>
    <automated>test -f /etc/nixos/zeroclaw/skills/README.md && grep -q "zeroclaw skills install" /etc/nixos/zeroclaw/skills/README.md && grep -q "SKILL.md" /etc/nixos/zeroclaw/skills/README.md && echo "PASS: skills/README.md exists with required content" || echo "FAIL"</automated>
  </verify>
  <done>File exists at /etc/nixos/zeroclaw/skills/README.md, contains zeroclaw skills CLI commands and SKILL.md format documentation sufficient for Kiro to create a new skill without human guidance</done>
</task>

<task type="auto">
  <name>Task 2: Create cron/README.md — full cron operational guide</name>
  <files>/etc/nixos/zeroclaw/cron/README.md</files>
  <action>
Create `/etc/nixos/zeroclaw/cron/README.md` as a complete operational guide for ZeroClaw cron. Kiro must be able to create, list, pause, resume, and remove cron jobs by reading this file alone.

Required sections:
1. Overview — SQLite-backed scheduler at ~/.zeroclaw/workspace/cron/jobs.db; no YAML files, no cron-sync; cron.enabled is true by default; cron commands can be shell or 'agent -m "prompt"' for AI-driven sessions
2. Schedule expressions — standard 5-field cron syntax table (min hour day month weekday), common examples (daily 9am, hourly, weekdays only); --tz IANA_TZ flag
3. CLI reference — table covering all cron subcommands: add, add-every, add-at, once, list, pause, resume, remove; include parameter format
4. Workflow examples — at least 3 concrete examples from RESEARCH.md: daily AI session at 9am Lima time, every-30-min task check, one-time job; show full zeroclaw cron add commands
5. Conventions — naming conventions for jobs (descriptive command strings, include 'agent -m' prefix for AI sessions); note that job IDs are assigned by SQLite and retrieved via cron list; brief schema note (jobs.db: cron_jobs table with id/expression/command/name columns) for power users
6. Anti-patterns — No YAML files, no cron-sync, no cron-manager, no direct DB writes

Section ordering and exact naming conventions are at executor's discretion. Do not duplicate skills content.
  </action>
  <verify>
    <automated>test -f /etc/nixos/zeroclaw/cron/README.md && grep -q "zeroclaw cron add" /etc/nixos/zeroclaw/cron/README.md && grep -q "SQLite" /etc/nixos/zeroclaw/cron/README.md && echo "PASS: cron/README.md exists with required content" || echo "FAIL"</automated>
  </verify>
  <done>File exists at /etc/nixos/zeroclaw/cron/README.md, contains zeroclaw cron CLI commands with schedule syntax and concrete examples sufficient for Kiro to create a new cron job without human guidance</done>
</task>

<task type="auto">
  <name>Task 3: Create CLAUDE.md — agent operational guide</name>
  <files>/etc/nixos/zeroclaw/CLAUDE.md</files>
  <action>
Create `/etc/nixos/zeroclaw/CLAUDE.md` as the primary agent reference for this repo. Audience: coding agents (Claude Code, etc.) and Kiro working on its own infrastructure. A fresh agent with no prior context should understand the full deployment model from reading this file once.

Required sections:

1. **Overview** — what this repo is (ZeroClaw NixOS module + Kiro's operational documents), single-sentence purpose, where it lives (/etc/nixos/zeroclaw/)

2. **Deployment Model** — table listing EVERY key file/directory with its deployment model and how to apply changes:
   | File / Directory | Model | How to Apply |
   Including these rows (use content from RESEARCH.md Architecture Patterns section):
   - module.nix → rebuild required → sudo nixos-rebuild switch --option eval-cache false --flake /etc/nixos#nixos
   - flake.nix (in /etc/nixos/) → rebuild required → direnv reload then rebuild
   - documents/*.md (all 6 identity docs) → live edit → edit in /etc/nixos/zeroclaw/documents/, commit to git — symlink means ZeroClaw sees changes immediately
   - skills/ (source) → deploy via CLI → zeroclaw skills install ./skills/my-skill
   - Skills at ~/.zeroclaw/workspace/skills/ → live after install
   - Cron jobs → live via CLI → zeroclaw cron add/remove/pause/resume
   - config.toml → rebuild required → rendered by Nix at build time

3. **File Map** — annotated directory tree of every important file/directory in the repo with one-liner purpose each. Include: CLAUDE.md, module.nix, documents/ (with all 6 identity docs listed), skills/, cron/, reference/, reference/upstream-docs/ (symlink to ~/Projects/zeroclaw/docs). Note which items are tracked in git vs managed by ZeroClaw runtime.

4. **Build Commands** — exact commands for coding agents:
   - nix flake check (syntax validation)
   - nix build .#nixosConfigurations.nixos.config.system.build.toplevel (full build test)
   - sudo nixos-rebuild switch --option eval-cache false --flake /etc/nixos#nixos (rebuild and activate)
   - Never run nix flake update (use direnv reload)

5. **Agent Operational Guide** — two subsections:
   a. **Kiro: Creating Skills** — steps from skills/README.md condensed: create dir → write SKILL.md → zeroclaw skills audit → zeroclaw skills install → zeroclaw skills list → git commit
   b. **Kiro: Managing Cron** — steps from cron/README.md condensed: zeroclaw cron add → zeroclaw cron list → use pause/resume/remove; note no files to commit
   c. **Coding Agents: Testing Changes** — for module.nix changes: nix flake check first, then nix build to verify, then sudo nixos-rebuild switch to activate; for documents: edit in repo, changes are immediately live

6. **Single Source of Truth Rule** — always edit source files in /etc/nixos/zeroclaw/, never edit ~/.zeroclaw/ directly; git is the source of truth for all content

Do NOT include IPC documentation — that is Phase 3 scope.

Section ordering and heading structure within the above are at executor's discretion.
  </action>
  <verify>
    <automated>test -f /etc/nixos/zeroclaw/CLAUDE.md && grep -q "rebuild" /etc/nixos/zeroclaw/CLAUDE.md && grep -q "live" /etc/nixos/zeroclaw/CLAUDE.md && grep -q "documents" /etc/nixos/zeroclaw/CLAUDE.md && echo "PASS: CLAUDE.md exists with deployment model content" || echo "FAIL"</automated>
  </verify>
  <done>File exists at /etc/nixos/zeroclaw/CLAUDE.md containing deployment model table (rebuild vs live-edit), complete file map, build commands, and agent operational guide; a fresh coding agent can understand the repo from reading it once</done>
</task>

</tasks>

<verification>
After all three tasks complete:

```bash
# Structural smoke tests
zeroclaw skills list && zeroclaw cron list

# File existence
test -f /etc/nixos/zeroclaw/skills/README.md && echo "skills/README OK" || echo "FAIL"
test -f /etc/nixos/zeroclaw/cron/README.md && echo "cron/README OK" || echo "FAIL"
test -f /etc/nixos/zeroclaw/CLAUDE.md && echo "CLAUDE.md OK" || echo "FAIL"

# Content sanity
grep -q "zeroclaw skills install" /etc/nixos/zeroclaw/skills/README.md && echo "skills workflow OK" || echo "FAIL"
grep -q "zeroclaw cron add" /etc/nixos/zeroclaw/cron/README.md && echo "cron workflow OK" || echo "FAIL"
grep -q "rebuild" /etc/nixos/zeroclaw/CLAUDE.md && echo "deployment model OK" || echo "FAIL"
```

Manual: Open CLAUDE.md and confirm the rebuild-vs-live-edit table lists module.nix, documents/*.md, config.toml, skills, and cron with correct models.
</verification>

<success_criteria>
1. skills/README.md, cron/README.md, and CLAUDE.md all exist
2. zeroclaw skills list and zeroclaw cron list return without errors
3. Kiro can follow skills/README.md to create a skill with zero ambiguity
4. Kiro can follow cron/README.md to add a cron job with zero ambiguity
5. Any coding agent reading CLAUDE.md immediately knows: documents/ = live edit, module.nix = rebuild required, config.toml = rebuild required, skills = CLI deploy
</success_criteria>

<output>
After completion, create `/etc/nixos/zeroclaw/.planning/phases/02-scaffolding-and-identity/02-01-SUMMARY.md`
</output>
