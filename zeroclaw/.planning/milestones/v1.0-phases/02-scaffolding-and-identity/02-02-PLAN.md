---
phase: 02-scaffolding-and-identity
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - /etc/nixos/zeroclaw/documents/AGENTS.md
autonomous: true
requirements: [IDN-01, IDN-02]

must_haves:
  truths:
    - "AGENTS.md contains no OpenClaw references — all tool names, paths, and commands reflect ZeroClaw's native systems"
    - "The System-First Rule table references zeroclaw cron, zeroclaw skills, and ZeroClaw memory — not cron-manager, skill-scaffold, or task-queue"
    - "The self-repair protocol references ZeroClaw memory for durable tracking — task-queue section is removed"
    - "The Hard Limits cron rule says to use zeroclaw cron CLI — not YAML files + cron-sync"
    - "All /etc/nixos/openclaw/ paths updated to /etc/nixos/zeroclaw/"
  artifacts:
    - path: "/etc/nixos/zeroclaw/documents/AGENTS.md"
      provides: "Kiro's complete operational constitution — autonomy rules, system-first rules, self-repair protocol — all ZeroClaw-native"
      contains: "zeroclaw cron"
  key_links:
    - from: "AGENTS.md System-First Rule table"
      to: "zeroclaw CLI commands"
      via: "cron → zeroclaw cron add, skills → zeroclaw skills install, tracking → zeroclaw memory"
      pattern: "zeroclaw cron|zeroclaw skills"
    - from: "AGENTS.md Hard Limits"
      to: "cron enforcement rule"
      via: "no YAML files — zeroclaw cron CLI only"
      pattern: "zeroclaw cron"
---

<objective>
Fully rewrite AGENTS.md to replace all OpenClaw-specific tool references with ZeroClaw-native equivalents.

Purpose: AGENTS.md is Kiro's operational constitution — it governs when to act autonomously, what tools to use, and how to handle failures. The entire System-First Rule table and Task Queue Protocol currently reference tools that don't exist in ZeroClaw (cron-manager, skill-scaffold, task-queue, cron-sync). This plan replaces every stale reference with ZeroClaw's native CLI and memory system.

Output: Updated /etc/nixos/zeroclaw/documents/AGENTS.md — clean break from OpenClaw, semantically correct for ZeroClaw
</objective>

<execution_context>
@/home/hybridz/.claude/get-shit-done/workflows/execute-plan.md
@/home/hybridz/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/etc/nixos/zeroclaw/.planning/ROADMAP.md
@/etc/nixos/zeroclaw/.planning/phases/02-scaffolding-and-identity/02-RESEARCH.md
@/etc/nixos/zeroclaw/documents/AGENTS.md

<interfaces>
<!-- What AGENTS.md currently contains (stale) vs what it must say (ZeroClaw-native) -->
<!-- From RESEARCH.md identity audit findings for AGENTS.md -->

Sections requiring full rewrite:
- System-First Rule table: replace cron-manager create → zeroclaw cron add; skill-scaffold create → zeroclaw skills install; task-queue add → zeroclaw memory (memory_store)
- "Never ask, just do" list: update cron jobs entry (YAML + cron-sync → zeroclaw cron add/remove/pause/resume); update path references (openclaw → zeroclaw)
- Hard Limits: cron rule → "Never create cron jobs via files or scripts — use zeroclaw cron CLI only (zeroclaw cron add/remove/pause/resume)"
- Self-Repair Protocol: replace task-queue references with ZeroClaw memory (memory_store/memory_recall) for durable issue tracking; add note that a dedicated skill for task tracking is v2 scope (CRN-01)
- Task Queue Protocol: remove entirely OR redesign as ZeroClaw memory protocol (preferred: redesign as "Durable Tracking Protocol" using memory_store)

Sections to preserve (semantically correct, only path updates):
- Core Directive — correct, no tool refs
- Approval Gate — correct
- Priority Stack — correct
- Hard Limits (non-cron items) — correct, only update openclaw paths
- Handling Uncertainty — correct
- Proactive Triggers — correct
- Error Handling — correct

Path update pattern (apply everywhere):
- /etc/nixos/openclaw/ → /etc/nixos/zeroclaw/
- /etc/nixos/openclaw/CLAUDE.md → /etc/nixos/zeroclaw/CLAUDE.md
- openclaw → zeroclaw (for all service/command references)

ZeroClaw memory commands (for Self-Repair and Durable Tracking):
- memory_store(key, value) — store durable record
- memory_recall(key) — retrieve stored record
- Use for: issue tracking, task queues, session continuity

Autonomy rule: Kiro retains full autonomy for cron/skills work — autonomy boundaries do not change, only the tooling changes.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Full rewrite of AGENTS.md for ZeroClaw-native tooling</name>
  <files>/etc/nixos/zeroclaw/documents/AGENTS.md</files>
  <action>
Read the current `/etc/nixos/zeroclaw/documents/AGENTS.md` end-to-end first. Then rewrite it with these changes:

**Sections requiring full replacement:**

1. **System-First Rule table** — Replace every stale tool with its ZeroClaw equivalent:
   - Cron management: `zeroclaw cron add/remove/pause/resume` (not cron-manager)
   - Skill management: `zeroclaw skills install/remove/audit` (not skill-scaffold)
   - Durable task tracking: `zeroclaw memory` (not task-queue — note that a dedicated skill is v2/CRN-01)
   - NixOS row: update path to `/etc/nixos/zeroclaw/CLAUDE.md`

2. **"Never ask, just do" list** — Update the cron entry: "Managing cron jobs (use `zeroclaw cron add/remove/pause/resume` — no YAML files, no cron-sync)"

3. **Hard Limits** — Replace the cron hard limit with: "Never create cron jobs via files, scripts, or any mechanism other than `zeroclaw cron` CLI. All cron state lives in SQLite — no YAML files."

4. **Self-Repair Protocol** — Replace any `task-queue add/resolve` steps with `zeroclaw memory` for durable record-keeping before attempting repair. The protocol logic (robust fix → workaround → ask user) stays the same; only the recording mechanism changes.

5. **Task Queue Protocol section** — Remove the task-queue-specific section. Replace with a brief "Durable Tracking" section documenting how to use `zeroclaw memory` for issue tracking and task continuity. Note that a dedicated task-queue skill is v2 scope (CRN-01). Keep the section short — this is not a full specification.

**Sections requiring only path updates:**
- All `/etc/nixos/openclaw/` → `/etc/nixos/zeroclaw/`
- All `openclaw-gateway` → `zeroclaw-gateway`
- Any other openclaw references found during full read

**Sections to preserve as-is (semantically correct):**
- Core Directive, Approval Gate, Priority Stack, Handling Uncertainty, Proactive Triggers, Error Handling — preserve content, update only openclaw paths if present

No backwards compatibility notes. No "this replaces OpenClaw" stubs. Write as if ZeroClaw is the only system that has ever existed.
  </action>
  <verify>
    <automated>grep -ri "openclaw" /etc/nixos/zeroclaw/documents/AGENTS.md && echo "FAIL: openclaw refs remain" || echo "PASS: no openclaw refs" && grep -q "zeroclaw cron" /etc/nixos/zeroclaw/documents/AGENTS.md && echo "PASS: zeroclaw cron present" || echo "FAIL: zeroclaw cron missing" && grep -q "zeroclaw skills" /etc/nixos/zeroclaw/documents/AGENTS.md && echo "PASS: zeroclaw skills present" || echo "FAIL: zeroclaw skills missing"</automated>
  </verify>
  <done>AGENTS.md contains no openclaw references; System-First Rule table references zeroclaw cron and zeroclaw skills; Self-Repair Protocol references zeroclaw memory; all paths point to /etc/nixos/zeroclaw/</done>
</task>

</tasks>

<verification>
After task completion:

```bash
# Zero openclaw refs
grep -ri "openclaw" /etc/nixos/zeroclaw/documents/AGENTS.md && echo "FAIL: stale refs" || echo "PASS: clean"

# ZeroClaw-native content present
grep -q "zeroclaw cron" /etc/nixos/zeroclaw/documents/AGENTS.md && echo "cron OK"
grep -q "zeroclaw skills" /etc/nixos/zeroclaw/documents/AGENTS.md && echo "skills OK"

# Runtime validation
zeroclaw skills list && zeroclaw cron list

# Wave audit (run after all Wave 1 plans complete)
grep -ri "openclaw" /etc/nixos/zeroclaw/documents/ && echo "AUDIT FAIL: openclaw refs remain" || echo "AUDIT PASS: all docs clean"
```
</verification>

<success_criteria>
1. Zero openclaw references in AGENTS.md
2. System-First Rule table contains zeroclaw cron and zeroclaw skills as the authoritative tools
3. No task-queue CLI commands anywhere in the document
4. Self-Repair Protocol uses zeroclaw memory for durable record-keeping
5. All paths point to /etc/nixos/zeroclaw/ — zero /etc/nixos/openclaw/ references
6. zeroclaw skills list and zeroclaw cron list continue to return without errors (no runtime regression)
</success_criteria>

<output>
After completion, create `/etc/nixos/zeroclaw/.planning/phases/02-scaffolding-and-identity/02-02-SUMMARY.md`
</output>
